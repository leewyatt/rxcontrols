package io.github.leewyatt.rxcontrols.bbcode;

/**
 * Whether a {@link RXListNode} is ordered (numbered) or unordered (bulleted).
 */
public enum RXListKind {

    /** Numbered list; rendered with sequential markers. */
    ORDERED,

    /** Bulleted list; rendered with bullet markers. */
    UNORDERED
}
