package io.github.leewyatt.rxcontrols.utils;

/**
 * Public UI utility class for the RXControls library.
 *
 * <p>Reserved for future general-purpose UI helpers (pixel snapping, bounds conversion,
 * scene-graph traversal, etc.). The legacy helpers that previously lived here were moved
 * into the only skin that consumed them (RXLrcViewSkin) during the 2026-05 review, pending
 * a behavioural rewrite.</p>
 */
public class UIUtil {

    private UIUtil() {
    }
}
